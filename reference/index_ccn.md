# Index a `ccn` object

Index a `ccn` object

## Usage

``` r
index_ccn(x)
```

## Arguments

- x:

  `<chr>` A vector of CCNs.

## Value

An S3 vector of class `<ccn_index>`

## Examples

``` r
if (FALSE) {
index_ccn(get_pin("ccn"))
}
```

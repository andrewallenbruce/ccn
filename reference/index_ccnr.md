# Index a `ccnr` object

Index a `ccnr` object

## Usage

``` r
index_ccnr(x)
```

## Arguments

- x:

  `<chr>` A vector of CCNs.

## Value

An S3 vector of class `<ccnr_index>`

## Examples

``` r
if (FALSE) {
index_ccnr(get_pin("ccn"))
}
```
